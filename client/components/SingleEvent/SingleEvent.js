import { connect } from 'react-redux';
import SingleEvent from './SingleEventDumb';

const mapDispatchToProps = dispatch => ({
});

export default connect(
  null,
  mapDispatchToProps
)(SingleEvent);
